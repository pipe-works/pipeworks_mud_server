# JS Behavior Harness

Lightweight JS behavior checks for browser modules that are otherwise hard to
cover with Python tests.

Run:

```bash
npm run test:js
```

Current scope:

- `users` page state-selection helpers
- `play` session-storage helpers
- `play` portal world-option normalization helpers
- `play` API parameter/error helpers
