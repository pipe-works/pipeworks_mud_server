/*
 * users.js
 *
 * Admin users view placeholder. Implementation will fetch and render user data.
 */

function renderUsers(root) {
  root.innerHTML = `
    <div class="panel wide">
      <h1>Users</h1>
      <p class="muted">User management interface coming next.</p>
    </div>
  `;
}

export { renderUsers };
